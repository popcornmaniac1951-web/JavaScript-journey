function countVowels(word){

    // vowels a,e,i,o,u
    let vowels = 'a';
    let result = word.includes(vowels);
    console.log(result);
    return 'it contain vowels';

}

let word = console.log(countVowels('adithyan'));